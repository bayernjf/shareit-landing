---
import { translations, type Locale } from '../i18n/ui';
import { getLocalizedUrl, getLocaleFromUrl } from '../i18n/utils';

interface Props {
  /** 当前 locale */
  locale: Locale;
  /** 页面相对路径，不含 locale 前缀，如 '' 或 'features' */
  path?: string;
  /** 覆盖标题（默认用站点名） */
  title?: string;
  /** 覆盖描述 */
  description?: string;
}

const { locale, path = '', title, description } = Astro.props;
const tr = translations[locale];
const site = Astro.site ?? new URL('https://shareit-landing.pages.dev');
const canonicalURL = new URL(`/${locale}${path ? '/' + path : ''}`, site);

// 跨语言 hreflang 互指
const alternates = (['zh', 'en'] as Locale[]).map((l) => ({
  hreflang: l === 'zh' ? 'zh-CN' : 'en-US',
  url: new URL(`/${l}${path ? '/' + path : ''}`, site).href,
}));

const pageTitle = title ?? tr.siteName;
const pageDescription = description ?? tr.siteDescription;

// JSON-LD 结构化数据
const orgJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Organization',
  name: 'ShareIt',
  url: site.href,
  description: tr.siteDescription,
};

const websiteJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  name: 'ShareIt',
  url: site.href,
  inLanguage: locale === 'zh' ? 'zh-CN' : 'en-US',
};

const appJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'SoftwareApplication',
  name: 'ShareIt',
  applicationCategory: 'MultimediaApplication',
  operatingSystem: 'Web, iOS, Android',
  description: tr.siteDescription,
  offers: [
    {
      '@type': 'Offer',
      name: tr.storageFreeName,
      price: '0',
      priceCurrency: 'CNY',
    },
  ],
};

const faqJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: tr.faqItems.map((item) => ({
    '@type': 'Question',
    name: item.q,
    acceptedAnswer: {
      '@type': 'Answer',
      text: item.a,
    },
  })),
};
---

<!-- 基础 meta -->
<title>{pageTitle}</title>
<meta name="description" content={pageDescription} />
<link rel="canonical" href={canonicalURL.href} />

<!-- hreflang 互指 -->
{alternates.map((alt) => (
  <link rel="alternate" hreflang={alt.hreflang} href={alt.url} />
))}
<link rel="alternate" hreflang="x-default" href={new URL('/zh', site).href} />

<!-- Open Graph -->
<meta property="og:type" content="website" />
<meta property="og:site_name" content="ShareIt" />
<meta property="og:title" content={pageTitle} />
<meta property="og:description" content={pageDescription} />
<meta property="og:url" content={canonicalURL.href} />
<meta property="og:locale" content={locale === 'zh' ? 'zh_CN' : 'en_US'} />
{alternates.map((alt) => (
  <meta property="og:locale:alternate" content={alt.hreflang.replace('-', '_')} />
))}
<meta property="og:image" content={new URL('/og-default.jpg', site).href} />

<!-- Twitter Card -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={pageTitle} />
<meta name="twitter:description" content={pageDescription} />
<meta name="twitter:image" content={new URL('/og-default.jpg', site).href} />

<!-- JSON-LD 结构化数据 -->
<script type="application/ld+json" set:html={JSON.stringify(orgJsonLd)} />
<script type="application/ld+json" set:html={JSON.stringify(websiteJsonLd)} />
<script type="application/ld+json" set:html={JSON.stringify(appJsonLd)} />
<script type="application/ld+json" set:html={JSON.stringify(faqJsonLd)} />
