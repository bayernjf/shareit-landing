# Cloudflare Pages 配置（可选 CLI 部署用）
# 主推：Cloudflare Pages 原生 Git 集成（在 Dashboard 连接 GitHub 仓库即可）
name = "shareit-landing"
compatibility_date = "2024-11-01"
pages_build_output_dir = "dist"

# 原生 Git 部署时在 Cloudflare Dashboard 配置：
#   构建命令: pnpm build
#   输出目录: dist
#   环境变量: NODE_VERSION=20
